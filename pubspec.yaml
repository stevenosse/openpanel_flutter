name: openpanel_flutter
description: "Openpanel is an open-source alternative to Mixpanel that combines the power of Mixpanel with the ease of Plausible. This package is not officially maintained by the Openpanel team."
version: 0.0.4
homepage: https://github.com/stevenosse/openpanel_flutter.git
topics: 
  - analytics

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  device_info_plus: ^10.1.0
  dio: ^5.4.3+1
  dio_smart_retry: ^6.0.0
  equatable: ^2.0.5
  logger: ^2.3.0
  package_info_plus: ^8.0.0
  referrer: ^0.0.2
  shared_preferences: ^2.2.3
  ua_client_hints: ^1.2.2
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
